#minimap-container
  position: fixed;
  left: 46px;
  bottom: 5px;
  height: 150px
  width: 150px

  #minimap
    background: #ffffcc
    position: relative
    opacity: 0.7
    z-index: 60

  #minimap-frame
    position: absolute
    border-style solid
    border-color black
    border-width 2px